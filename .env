# Read by docker compose for variable substitution in docker-compose.yml.
# Must match GATE_SYNC_SECRET on the cloud backend exactly, or the gate-staff
# pull in sync.py returns 401 and staff cannot log in at the gate offline.
# Rotated 2026-08-15 — the previous value is public in aleson-backend's git
# history.
GATE_SYNC_SECRET=jwmSstjaRKXo7WHR86Z7gwnNGmoAbd9lFUKpEWL6lLqQJyE6
